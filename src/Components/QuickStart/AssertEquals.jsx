import React, { useState } from 'react'
import styles from './QuickStart.module.scss'


export default () => (
    <div className={styles['section-comment']}>
        One of the actions we want to make at the of scenario is controll whether all data in system have correct values. In the end of the quick start after all commands was executed text on left panel should change value to Complited. Make sure it is and let's start with automation of your Salesforce application with Overlight!    
    </div>
)